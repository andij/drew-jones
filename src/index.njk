---
title: drew-jones.com
tagline: engineering design systems
description: and systems thinker
layout: base.njk
---

<nav class="navigation">
  {% for link in links %}
  <div class="menu">
    <div class="main">
      <div class="icon" tabindex="0"><i data-lucide="{{ link.icon }}" aria-hidden="true"></i></div>
      <a class="link" href="{{ link.url }}" title="{{ link.description }}">{{ link.title }}</a>
    </div>
    {% if link.submenu %}
    <div class="sub">
      {% for sublink in link.submenu.links %}
        <a href="{{ sublink.url }}">{{ sublink.title }}</a>
      {% endfor %}
      {% for paragraph in link.submenu.paragraphs %}
        <p>{{ paragraph }}</p>
      {% endfor %}
      <i class="x" data-lucide="x" aria-hidden="true"></i>
    </div>
    {% endif %}
  </div>
  {% endfor %}
</nav>

<div class="drew">
  <h1>{{ title }}</h1>
  <p>{{ tagline }}</p>
</div>
